# Base URL of the share Worker (see worker/). When set, the result screen offers
# a "Get share link" button that uploads the result and returns a public link
# that stays live for 72 hours. Leave unset to fall back to native file sharing.
VITE_SHARE_API_BASE=https://boothique-share.example.workers.dev
